# IDEA: search regular expressions

## Regex - Find matching words that don't begin with a specific prefix in IntelliJ IDEA'a Find in path | SO

https://stackoverflow.com/questions/53053019/regex-find-matching-words-that-dont-begin-with-a-specific-prefix-in-intellij

Search without leading `+`:

```regex
^phone|[^+]phone
```

