# Архитектура: движки правил

## Плюсы rules engine

> 🗣️️ Какие плюсы у rules engine вроде https://github.com/hyperjumptech/grule-rule-engine по сравнению с чтением и запуском Lua скриптов из базы?

🤖 DeepSeek, 2026.09:

### Rete-алгоритм

Grule (как и Drools) использует RETE-алгоритм — эффективный механизм сопоставления фактов с правилами.

Представьте 1000 правил:

```
rule "R1" { when A && B then ... }
rule "R2" { when B && C then ... }
rule "R3" { when C && D then ... }
...
```

RETE-алгоритм:

- Строит граф условий
- Переиспользует общие части (B проверяется один раз)
- Активирует только релевантные правила
- Не перебирает все 1000 правил при каждом изменении

### Безопасность

Lua-скрипт — это код. Он может:

- Читать файлы (io.open)
- Выполнять команды ОС (os.execute)
- Бесконечно циклить (while true do end)
- Выделять много памяти
- Падать с ошибкой

Grule DSL — это ограниченный язык. Он:

- Не имеет доступа к файловой системе
- Не имеет доступа к ОС
- Не может создавать циклы (по дизайну)
- Работает только с фактами (facts)
- Проще в освоении и проверке бизнес-аналитиками

## Алгоритм Rete / Википедия

https://ru.wikipedia.org/wiki/Алгоритм_Rete

Rete — эффективный алгоритм сопоставления с образцом для продукционных систем, экспертных систем и баз знаний, созданный Чарльзом Форги из Университета Карнеги — Меллона. Впервые был описан в рабочем документе 1974 года, затем в докторской диссертации 1979 года и в статье 1982 года (см. Ссылки). Rete стал основой многих популярных экспертных систем, включая CLIPS, Jess, Drools, BizTalk Rules Engine и Soar.

При использовании Rete экспертная система строит специальный граф или префиксное дерево, узлам которого соответствуют части условий правил. Путь от корня до листа образует полное условие некоторой продукции. В процессе работы каждый узел хранит список фактов, соответствующих условию. При добавлении или модификации факта он прогоняется по сети, при этом отмечаются узлы, условиям которых данный факт соответствует. При выполнении полного условия правила, когда система достигает листа графа, правило выполняется.

Алгоритм Rete жертвует объёмом памяти ради скорости. В большинстве случаев скорость возрастает на порядки (так как эффективность теоретически не зависит от числа правил в системе).